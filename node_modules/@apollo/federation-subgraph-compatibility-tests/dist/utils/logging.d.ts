/// <reference types="node" />
import { Debugger } from 'debug';
import { Writable } from 'stream';
import { TestResults } from '../testRunner';
/**
 * Log message with a timestamp.
 */
export declare function logWithTimestamp(message: string): void;
/**
 * Log compatibility test results to console.
 */
export declare function logResults(testResults: TestResults): void;
/**
 * Writable debug stream that uses Debugger to indicate whether message should be logged or not.
 */
export declare function writeableDebugStream(debug: Debugger): Writable;
//# sourceMappingURL=logging.d.ts.map