export interface DockerConfig {
    kind: 'docker';
    /** Docker compose file */
    composeFile: string;
    /** Schema file to be used for composition */
    schemaFile: string;
    /** GraphQL endpoint path */
    path?: string;
    /** HTTP server port */
    port?: string;
    /** Report format */
    format: string;
    /** Boolean flag to indicate whether any failing required test should fail the script. */
    failOnRequired: boolean;
    /** Boolean flag to indicate whether any failing test should fail the script. */
    failOnWarning: boolean;
}
export interface Pm2Config {
    kind: 'pm2';
    /** Product subgraph endpoint */
    endpoint: string;
    /** Schema file, if omitted composition will fallback to introspection */
    schemaFile?: string;
    /** PM2 configuration file */
    configFile?: string;
    /** Report format */
    format: string;
    /** Boolean flag to indicate whether any failing required test should fail the script. */
    failOnRequired: boolean;
    /** Boolean flag to indicate whether any failing test should fail the script. */
    failOnWarning: boolean;
}
/**
 * Starts supergraph using PM2 or Docker Compose.
 *
 * @returns shutdown hook for PM2/Docker Compose
 */
export declare function startSupergraph(config: DockerConfig | Pm2Config): Promise<() => Promise<void>>;
//# sourceMappingURL=startSupergraph.d.ts.map