"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const client_1 = require("../utils/client");
const schemaComparison_1 = require("../utils/schemaComparison");
test('introspection', async () => {
    const serviceSDLQuery = await (0, client_1.productsRequest)({
        query: 'query { _service { sdl } }',
    });
    expect(serviceSDLQuery.data).toMatchObject({
        _service: {
            sdl: expect.stringContaining('type Query'),
        },
    });
    const basicallyTheSame = (0, schemaComparison_1.compareSchemas)(serviceSDLQuery.data?._service?.sdl);
    expect(basicallyTheSame).toBe(true);
});
//# sourceMappingURL=introspection.test.js.map